import styled from 'styled-components';

export const Container = styled.section``;

export const Center = styled.div`
    max-width: ${(props) => props.theme.sizes.containerWidth};
    margin: 0 auto;
    padding: 0 1.6rem;
`;

export const DiskButtons = styled.div``;
